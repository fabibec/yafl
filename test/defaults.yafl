{-
    Defaults Test
    Tests simple default arguments.
-}

fn start() -> none {
    foo(10);
    println(>>---<<);
    foo(10, 30);
}

fn foo(int : |a|, int : |b| <- 20) -> none {
    println(to_str(|a|));
    println(to_str(|b|));
}
