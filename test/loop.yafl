fn start() -> int {
    range'int |r| <- range(-1, 2);
    int |i|;
    for |i| in |r| {
        println(>>Index: << + to_str(|i|));
    }
    println(|i|);
    for int |i| in >>Hello<< {
        println(|i|);
    }
    println(|i|);
    ret 0;
}
