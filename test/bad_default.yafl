{-
    Bad default Test
    Some stuff that should fail
-}

fn test(int: |a| <- 5, int: |b|) -> none {
    println(|a| + |b|);
}

fn test(int: |a| <- >>bad<<) -> none {
    println(|a|);
}

fn start() -> none {
    test(1, 2);
    test(5);
}
