fn start() -> int {
    str |s| <- >>The answer is << + to<bool>(-10#32);
    print(|s|);
    ret 0;
}

{-
types: bool, int, str, (float)

bool -> int No (should work, 0 & 1);
bool -> str Yes (but print 1 should print Yes);
bool -> float (0.0 or 1.0)

int -> bool (use != 0 to determine)
int -> str Yes (not with negative numbers, works always)
int -> float (should always work)

float -> bool (use != 0 to determine)
float -> int (cut)
float -> str (should always work print every digit that isnt zero);

str -> bool >>No<< to 0 >>Yes<< to 1 (
    everything else error >>No<<
)
str -> int 0
str -> float 0.0

-}
